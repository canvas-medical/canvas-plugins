name: Pre-commit auto-update
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  pre-commit-auto-update:
    uses: canvas-medical/workflows/.github/workflows/pre-commit-auto-update.yml@main
    with:
      PR_TITLE: "chore: auto-update pre-commit hooks"
      PR_TEAM_REVIEWERS: sdk-team
    secrets: inherit
