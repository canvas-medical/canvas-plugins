note_uuid_tester
================

## Description

Adds three buttons that do the following:
1. Creates a note with no specified uuid
2. Creates a note with a specified uuid, and a command inside that note
3. Creates a note with a hard-coded uuid (can only be called once)

