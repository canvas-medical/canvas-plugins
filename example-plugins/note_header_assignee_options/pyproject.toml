[project]
authors = [
  {email = "engineering@canvasmedical.com", name = "Canvas Team"}
]
dependencies = [
  "canvas[test-utils]"
]
description = "Reorders the note header assignee dropdown so teams appear before individual staff"
license = "MIT"
name = "note_header_assignee_options"
readme = "note_header_assignee_options/README.md"
requires-python = ">=3.11"
version = "0.0.1"

[tool.pytest.ini_options]
python_files = ["*_tests.py", "test_*.py", "tests.py"]
